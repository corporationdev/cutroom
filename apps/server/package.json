{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@cutroom/api": "workspace:*",
    "@cutroom/auth": "workspace:*",
    "@cutroom/db": "workspace:*",
    "@cutroom/env": "workspace:*",
    "alchemy": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "effect": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@cutroom/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:",
    "wrangler": "^4.77.0"
  }
}
